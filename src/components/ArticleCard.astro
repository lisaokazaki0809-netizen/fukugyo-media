---
import { getCategoryName, getCategoryColor, getCategoryBgColor, getCategoryImage } from '../lib/categories';

interface Props {
  title: string;
  description: string;
  pubDate: Date;
  category: string;
  slug: string;
  featured?: boolean;
  index?: number;
}

const { title, description, pubDate, category, slug, featured = false, index = 0 } = Astro.props;
const catColor = getCategoryColor(category);
const catBgColor = getCategoryBgColor(category);
const catImage = getCategoryImage(category, index);

const formattedDate = pubDate.toLocaleDateString('ja-JP', {
  year: 'numeric',
  month: '2-digit',
  day: '2-digit',
});
---

<article class="group">
  <a href={`/posts/${slug}/`} class="block">
    <div class="bg-white rounded-xl overflow-hidden transition-all duration-200 hover:shadow-md border border-transparent hover:border-border">
      <div class="overflow-hidden bg-bg-muted">
        <img
          src={catImage}
          alt=""
          class="w-full h-auto block group-hover:scale-105 transition-transform duration-300"
          loading="lazy"
        />
      </div>
      <div class="p-4">
        <div class="flex items-center gap-2 mb-2">
          <span
            class="text-[11px] font-bold uppercase tracking-wide px-2 py-0.5 rounded shrink-0"
            style={`color: ${catColor}; background-color: ${catBgColor}`}
          >
            {getCategoryName(category)}
          </span>
          <time datetime={pubDate.toISOString()} class="text-[12px] text-text-muted">
            {formattedDate}
          </time>
        </div>
        <h3 class="text-[15px] font-bold text-text leading-snug group-hover:text-accent transition-colors mb-1.5 line-clamp-2">
          {title}
        </h3>
        <p class="text-[13px] text-text-secondary line-clamp-2 leading-relaxed">
          {description}
        </p>
      </div>
    </div>
  </a>
</article>
